import React from 'react'

const Navbar = (props) => {

    return (
        <div id={`navbar`} className={`${ props.hidden ? "hidden": "not-hidden"}`}>
            
        </div>
    )
}

export default Navbar
